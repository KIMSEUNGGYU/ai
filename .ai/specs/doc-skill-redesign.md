---
title: /doc 스킬 재설계
date: 2026-04-05
status: draft
---

# /doc 스킬 재설계

## 목적

대화 내용을 분석해서 적절한 유형과 구조로 문서를 자동 작성하는 통합 스킬.
기존 `/doc` + `/note`를 흡수. `/study`는 별도 유지.

## 트리거

- "정리해줘", "정리해", "해당 내용 정리해줘", "문서로 만들어줘"
- `/doc`

## 문서 유형 (4가지)

대화 내용을 분석해서 자동 판별. 판별 결과를 사용자에게 한 줄로 알려줌.
판별이 애매하면 사용자에게 질문.

### project-journal

구현 과정 + 의사결정이 있는 작업 기록.

```markdown
# {제목}

## 배경
- 왜 이 작업이 필요했는가 (비즈니스 맥락)

## 최종 구조
- 완성된 아키텍처/설계 (코드, 다이어그램, 표 등)

## 핵심 구현
- 주요 코드/설정
- 다시 봤을 때 맥락을 복원할 수 있는 수준으로 포함

## 고민 포인트
### Q1. 한 줄 질문
**문제** — 뭐가 문제였는지
**검토한 방법** — 표로 비교 (방식/설명/장단점)
**결론** — 선택 + 근거
```

### troubleshooting

에러/버그 해결 과정 기록.

```markdown
# {제목}

## 증상
- 어떤 에러/문제가 발생했는가

## 원인
- 왜 발생했는가 (근본 원인)

## 해결
- 구체적 조치 (코드/설정)

## 교훈
- 재발 방지 또는 알게 된 점
```

### decision

구현 없이 기술 결정만 한 경우.

```markdown
# {제목}

## 배경
- 왜 이 결정이 필요했는가

## 선택지
| 방식 | 설명 | 장점 | 단점 |
...

## 결정
- 무엇을 선택했는가

## 근거
- 왜 이것을 선택했는가
```

### guide

설정/구축 절차 기록 (에러 해결이 아닌 처음부터 세팅).

```markdown
# {제목}

## 개요
- 무엇을, 왜 하는가

## 절차
1. 단계별 구체적 실행 방법

## 주의사항
- 빠지기 쉬운 함정, 전제 조건
```

## 작성 원칙 (모든 유형 공통)

technical-writing.dev + 사용자 실제 패턴에서 추출.

### 구조 원칙
1. **최종 결과물 먼저** — 결론/완성된 구조를 앞에, 과정은 뒤에
2. **한 문서 한 주제** — 여러 주제가 섞이면 분리
3. **예측 가능한 구성** — 유형별 정해진 구조를 따름

### 깊이 원칙
4. **"왜?" 필수** — 모든 결정/선택에 근거 포함. 표면적 서술 금지
5. **고민 과정 누락 금지** — 대화에서 나온 검토 내용, 기각된 대안도 기록
6. **비교표 활용** — 선택지가 2개 이상이면 표로 정리
7. **맥락 복원 가능한 깊이** — 코드는 줄이되, 다시 봤을 때 "왜 이렇게 했지?"가 남으면 안 됨

### 문장 원칙
8. **구체적으로** — "여러 가지 방법" → 구체적 방법명 나열
9. **주체 분명히** — "처리된다" → "Next.js가 처리한다"
10. **불필요한 정보 제거** — 자명한 설명 생략

## 저장 위치 규칙

pc-map.md 기반 자동 판단. AI가 제안하고 사용자 확인 후 저장.

| 내용 성격 | 저장 위치 | 예시 |
|----------|----------|------|
| 특정 프로젝트 작업 | 해당 프로젝트 `.ai/notes/` | 하네스 구현 과정 |
| 프로젝트 축적 지식 | `~/hq/10_projects/{프로젝트}/` | ishopcare 도메인 정리 |
| 학습/기술 참고 | `~/hq/30_learn/` | React 패턴 |
| 시스템/환경설정 | `~/hq/40_system/` | Zscaler SSL |
| 분류 불명확 | 사용자에게 질문 | — |

frontmatter 필수: tags, date, status.

## 실행 흐름

```
사용자: "정리해줘" / "/doc"
  │
  ▼
1. 대화 내용 분석
   - 어떤 주제인가
   - 어떤 결정/고민/해결이 있었는가
  │
  ▼
2. 유형 판별 + 저장 위치 제안 (한 줄)
   "project-journal로 ~/hq/10_projects/ishopcare/에 저장합니다"
  │
  ▼
3. 사용자 확인
   - OK → 작성 진행
   - 수정 → 반영 후 진행
  │
  ▼
4. 문서 작성
   - 해당 유형 구조 적용
   - 작성 원칙 10개 적용
  │
  ▼
5. 저장
   - frontmatter 포함
   - 파일 저장 후 경로 알려줌
```

사용자 개입은 3번(유형+위치 확인) 한 번만. 나머지는 자동.

## 참고

- technical-writing.dev (Toss 테크니컬 라이팅 가이드)
- 사용자 예제: Framer→Next.js 전환 문서, Mixpanel A/B 테스트 인프라 문서
