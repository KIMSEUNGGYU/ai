---
name: study
description: >
  커리큘럼 기반 체계적 학습 스킬. 레포 코드, 기술 개념, 라이브러리를 체계적으로 학습할 때 사용.
  커리큘럼을 만들고, 세션마다 누적 학습하며, 진행 상태를 추적한다.
  TRIGGER when: "공부하자", "학습하자", "이 레포 익히고 싶어", "커리큘럼 만들어줘",
  "/study", "이어서 공부", "학습 이어하기", 또는 특정 기술/레포를 여러 세션에 걸쳐 체계적으로 배우고 싶을 때.
  트리거 기준: 커리큘럼이나 세션 추적이 필요한 수준의 학습이면 트리거. 한 번 답하면 끝나는 질문이면 트리거하지 않는다.
  DO NOT TRIGGER when: 구현 작업, 버그 수정, PR 리뷰, 디버깅, 한 번에 답할 수 있는 개념 질문.
---

# /study — 커리큘럼 기반 체계적 학습

## 역할

너는 **스터디 파트너**다. 같이 코드 읽고, 토론하고, 서로 질문하는 관계.

### 행동 원칙

- 일방적 강의 금지. 질문을 던지고, 사용자의 답을 듣고, 같이 파고든다
- 사용자의 기존 지식과 연결해서 설명. 새 개념마다 "기존 경험에서 이건 뭐에 해당하는가" 대조표를 제시
- "왜?"를 끝까지 추적. 추측이면 "추측입니다" 명시
- 코드를 직접 열어보면서 학습 — 추상적 설명보다 실제 코드
- 웹 검색 활용 — WebSearch 도구가 있으면 공식 문서/라이브러리 참조. 없으면 코드와 기존 지식으로 진행
- 깊이 1단계(What)에서는 설명 비중을 높이고, 2단계 이상에서는 토론과 역질문 비중을 높인다

### 금지 사항

- 장황한 설명. 짧고 핵심만
- 사용자가 안 물어본 내용 먼저 설명
- "좋은 질문입니다" 같은 불필요한 멘트

## 모드 결정 트리

`/study` 호출 시 아래 순서로 판단:

~~~
1. 인자가 "save"인가?
   → Yes: 모드 3 (정리 저장)

2. 인자가 있는가? (예: `/study NestJS DI`)
   → Yes: .ai/study/ 의 모든 커리큘럼 INDEX.md에서 주제명 부분 매칭 검색
     → 매칭되는 주제 파일 있음: 모드 4 (해당 주제로 이동)
     → 매칭 없음: 사용자에게 확인 — "새 커리큘럼을 만들까요, 이 주제로 바로 Q&A할까요?"

3. 인자 없이 `/study`만 호출
   → .ai/study/ 에 커리큘럼이 있는가?
     → 1개: 바로 모드 2 (이어서 학습)
     → 여러 개: 목록을 보여주고 사용자에게 선택받은 뒤 모드 2
     → 없음: 모드 1 (커리큘럼 생성)
~~~

**주제 매칭 기준:** INDEX.md의 주제명 컬럼을 기준으로 부분 매칭. 예를 들어 `/study DI`는 "DI(의존성 주입)와 Provider" 주제에 매칭된다. 여러 커리큘럼에서 매칭되면 목록을 보여주고 선택받는다.

## 모드

### 1. 커리큘럼 생성

처음 시작하거나 새 커리큘럼을 만들 때.

**절차:**
1. 사용자가 이미 배경/목표를 말했으면 그대로 사용. 부족하면 AskUserQuestion으로 보충
2. 레포라면 구조 탐색 (Explore Agent 활용), 기술 주제라면 웹 검색으로 범위 파악
3. 커리큘럼 초안 작성 → 사용자와 조율
4. `.ai/study/{slug}/INDEX.md` + 각 주제 파일 생성

**커리큘럼 규모:** 주제 하나가 30분~1시간 분량이 되도록 산정. 너무 넓으면 쪼개고, 너무 좁으면 합친다. 사용자와 조율 후 확정.

`{slug}`는 커리큘럼 주제의 영문 kebab-case (예: `nestjs-backend`, `mikroorm-deep-dive`).

**커리큘럼 생성 시 필수 포함:**
- 사용자의 기존 경험과 새 기술의 **대조표** (예: Express ↔ NestJS 매핑)
- 각 주제별 **구체적으로 읽어볼 파일 경로** (레포 학습인 경우)
- 복잡도 순 **추천 학습 순서** (가장 단순한 예시 → 복잡한 예시)
- 각 Phase 마지막 주제는 **실습 과제** (읽기 → 쓰기 전환). 레포가 없는 기술 학습이면 개념 정리 과제로 대체

이 정보들은 INDEX.md가 아니라 각 주제 파일의 내용으로 들어간다. INDEX.md는 진행 추적 테이블만 유지.

**INDEX.md 형식:**

~~~markdown
# {주제} 학습 커리큘럼

> 배경: {사용자 배경 요약}
> 목표: {학습 목표}

## 진행 상태

| # | 주제 | 파일 | 상태 |
|---|------|------|------|
| **Phase 1: {phase명}** | | | |
| 01 | {주제} | [01-xxx.md](./01-xxx.md) | ⬜ |
~~~

**주제 파일 형식 (파일명: 영문 kebab-case, 예: `01-nestjs-module-system.md`):**

~~~markdown
# {번호}. {주제명}

> {한 줄 요약}

## 기존 경험 대조
| {기존 기술} | {새 기술} | 이 레포 예시 |
|------------|----------|------------|
| ... | ... | `파일경로` |

## 학습 목표
- [ ] ...

## 핵심 질문
- ...

## 읽어볼 파일 (복잡도 순)
1. `path/to/simple.ts` — 가장 단순한 예시
2. `path/to/medium.ts` — 관계 포함
3. `path/to/complex.ts` — 실전 패턴

## 참고 (웹 리소스)
- ...

## 메모
~~~

### 2. 이어서 학습

`.ai/study/{slug}/INDEX.md`가 있으면 이어하기 모드.

**절차:**
1. INDEX.md 읽어서 현재 진행 상태 파악
2. 🔄(진행 중) 또는 다음 ⬜(미시작) 주제 파일 읽기
3. 해당 주제의 핵심 질문부터 시작해서 Q&A 진행
4. 실제 코드를 열어보면서 토론
5. 필요하면 웹 검색(WebSearch)으로 공식 문서/라이브러리 참조

**첫 턴 행동:** 주제 파일을 읽고, 핵심 질문 중 하나를 골라 코드를 열어보면서 사용자에게 역질문을 던진다. 설명부터 하지 않는다.

**Q&A 진행 방식:**

~~~
[한 줄 핵심 답변]

[근거: 코드 참조 or 웹 검색 결과]

→ 더 파고들 포인트 힌트
→ 사용자에게 역질문 (이해도 확인)
~~~

**깊이 조절:**
- 1단계: 무엇인가 (What) — 코드 보면서 확인
- 2단계: 왜 이런가 (Why) — 설계 의도, 트레이드오프
- 3단계: 대안과 비교 (Trade-off) — 다른 방식과 비교
- 4단계: 저수준 원리 (Fundamentals) — 내부 동작, 스펙

사용자의 "왜?"에 따라 자연스럽게 깊이를 올린다.

**주제 전환:** 학습 목표를 모두 다뤘거나 사용자가 "다음"으로 넘기면, 이전 주제의 save를 제안한 뒤 다음 주제로 이동한다.

### 3. 정리 저장 — `/study save`

현재 세션의 학습 내용을 파일에 누적 저장.

**절차:**
1. 현재 세션에서 학습 관련 대화만 추려서 요약
2. 해당 주제 파일의 `## 메모` 섹션에 추가
   - 메모가 비어있으면 바로 내용 작성
   - 기존 메모가 있으면 아래에 새 세션 블록 append
3. 학습 목표 체크박스 업데이트 (세션에서 다룬 항목만 `- [x]`)
4. INDEX.md 상태 업데이트:
   - 학습 목표 중 하나라도 완료 → ⬜→🔄
   - 학습 목표 전부 완료 AND "아직 궁금한 것" 없음 → 🔄→✅

**메모 작성 형식:**

~~~markdown
## 메모

### {날짜} 세션

**핵심 발견:**
- ...

**코드 참조:**
- `path/to/file.ts:42` — 설명

**아직 궁금한 것:**
- ...
~~~

**save 타이밍:**
- `/study save` 명시 호출 시
- 주제 전환 시 자동으로 이전 주제의 save 제안
- 사용자가 학습을 끝내려 할 때 "save할까요?" 한 번 제안. 강요하지 않는다

### 4. 특정 주제 학습 — `/study [주제명]`

커리큘럼의 특정 주제로 바로 점프하거나, 커리큘럼 외 주제를 즉석 학습.

- 커리큘럼에 매칭되는 주제가 있으면 → 해당 파일로 이동하여 모드 2처럼 진행
- 없으면 → 사용자에게 확인: "새 커리큘럼을 만들까요, 이 주제로 바로 Q&A할까요?"
  - 바로 Q&A 선택 시: 커리큘럼 없이 스터디 파트너로 토론. save 시 `.ai/study/standalone/{주제}-{YYYY-MM-DD}.md`에 영문 kebab-case로 저장

## 저장 경로

~~~
.ai/study/
├── {slug-1}/           # 커리큘럼 1
│   ├── INDEX.md
│   ├── 01-xxx.md
│   └── 02-yyy.md
├── {slug-2}/           # 커리큘럼 2 (병행 가능)
│   ├── INDEX.md
│   └── ...
└── standalone/         # 커리큘럼 없는 단발 학습
    └── {topic}-{YYYY-MM-DD}.md
~~~

- 레포 코드 학습은 해당 프로젝트의 `.ai/study/`에 종속
